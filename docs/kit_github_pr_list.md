## kit github pr list

List pull requests

### Synopsis

List pull requests for a repository.

```
kit github pr list [flags]
```

### Options

```
  -h, --help           help for list
      --json           output raw JSON
  -o, --owner string   repository owner
  -p, --page int       page number (default 1)
  -n, --per-page int   number of results per page (default 30)
  -q, --quiet          minimal output
  -r, --repo string    repository name
  -s, --state string   PR state: open, closed, all (default "open")
```

### Options inherited from parent commands

```
      --config string   config file (default is $HOME/.kit/kit.yaml)
  -v, --verbose         verbose output
```

### SEE ALSO

* [kit github pr](kit_github_pr.md)	 - Pull request commands

